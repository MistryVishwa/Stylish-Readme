{
  "name": "readme-forge",
  "version": "2.1.0",
  "private": true,
  "description": "Stylish Readme — dynamic SVG widgets for GitHub READMEs",
  "devDependencies": {
    "serve": "^14.2.0",
    "vitest": "^1.0.0"
  },
  "scripts": {
    "dev": "serve -s . -l 5000",
    "start": "npm run dev",
    "test": "vitest run",
    "coverage": "vitest run --coverage"
  },
  "engines": {
    "node": ">=18"
  }
}
