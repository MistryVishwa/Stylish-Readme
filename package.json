{
  "name": "readme-forge",
  "version": "2.1.0",
  "private": true,
  "description": "Stylish Readme — dynamic SVG widgets for GitHub READMEs",
  "main": "server.js",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "test": "vitest run",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^1.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
